# Copy this file to /var/www/VisionTemplate/.env on the VPS.
# Fill in strong deployment values there. Do not commit the real .env file.

POSTGRES_USER=vision_user
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD
POSTGRES_DB=vision_template
DATABASE_URL=postgresql://vision_user:CHANGE_ME_STRONG_PASSWORD@vision_postgres:5432/vision_template
